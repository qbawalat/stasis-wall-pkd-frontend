import React, { FunctionComponent } from 'react';
import AutofillInput from '../../configs/components/inputs/AutofillInput/AutofillInput';

const SearchBar: FunctionComponent = () => {
  return <AutofillInput />;
};

export default SearchBar;
